# Руководство по миграции на новую архитектуру

## Шаг 1: Проверка работоспособности

После рефакторинга проект должен работать без изменений. Проверьте:

1. **Главная страница** - `http://yoursite.com/`
2. **Авторизация** - `http://yoursite.com/login.php`
3. **Регистрация** - `http://yoursite.com/register.php`
4. **Список торрентов** - `http://yoursite.com/torrents.php`
5. **Форум** - `http://yoursite.com/forum.php`

Если все работает - отлично! Переходим к следующему шагу.

---

## Шаг 2: Миграция на новый header (опционально)

Новый `header_new.php` содержит чистую презентационную логику без создания таблиц и директорий.

### Вариант А: Полная замена

```bash
# Сделайте резервную копию
cp templates/header.php templates/header_old.php

# Замените старый header на новый
mv templates/header_new.php templates/header.php
```

### Вариант Б: Постепенная миграция

Оставьте оба файла и используйте новый только на новых страницах:

```php
// Для новых страниц используйте новый header
require_once __DIR__ . '/templates/header_new.php';
```

---

## Шаг 3: Обновление конфигурации

Убедитесь, что `config.php` содержит все необходимые параметры:

```php
<?php
declare(strict_types=1);

return [
    'db' => [
        'host' => 'localhost',
        'user' => 'root',
        'pass' => '',
        'name' => 'torrentbg',
        'charset' => 'utf8mb4',
    ],
    'site' => [
        'name' => 'Torrent Tracker',
        'default_lang' => 'en',
        'default_style' => 'light',
        'installed' => true,
    ],
];
```

---

## Шаг 4: Проверка ошибок

### Включите отображение ошибок временно

В начале `bootstrap.php` (только для разработки):

```php
<?php
error_reporting(E_ALL);
ini_set('display_errors', '1');

// ... остальной код
```

⚠️ **ВАЖНО:** Отключите это на продакшене!

### Проверьте логи PHP

```bash
# Linux/Mac
tail -f /var/log/php/error.log

# Windows (OSPanel)
# Смотрите logs в папке OSPanel
```

---

## Шаг 5: Известные проблемы и решения

### Проблема 1: "Class 'Application' not found"

**Причина:** Autoloader не загрузил класс.

**Решение:**
```php
// Убедитесь что bootstrap.php подключен первым
require_once __DIR__ . '/includes/bootstrap.php';
```

### Проблема 2: "Call to undefined function formatBytes()"

**Причина:** Файл `functions.php` не подключен.

**Решение:**
```php
// В bootstrap.php уже есть require для functions.php
// Если используете файл без bootstrap, добавьте:
require_once __DIR__ . '/includes/functions.php';
```

### Проблема 3: Двойное HTML-экранирование

**Причина:** Используется старый код с автоматическим экранированием.

**Решение:**
```php
// Старый код (двойное экранирование):
<?= htmlspecialchars($lang->get('key')) ?>

// Новый код (правильно):
<?= htmlspecialchars($lang->get('key')) ?>  // или
<?= $lang->e('key') ?>  // автоматическое экранирование
```

### Проблема 4: Сессия уже запущена

**Причина:** `session_start()` вызывается дважды.

**Решение:** Bootstrap уже запускает сессию, удалите дублирующий `session_start()`.

---

## Шаг 6: Очистка старого кода

После успешной миграции можно удалить неиспользуемые файлы:

```bash
# НЕ ДЕЛАЙТЕ ЭТО СРАЗУ! Сначала убедитесь, что все работает.

# Удалить старый header (если используете новый)
rm templates/header_old.php

# Удалить test файлы (если они есть)
rm test.php test-path.php
```

---

## Шаг 7: Оптимизация производительности

### 1. Включите OPcache (если еще не включен)

В `php.ini`:
```ini
opcache.enable=1
opcache.memory_consumption=128
opcache.max_accelerated_files=10000
```

### 2. Настройте кэширование статистики

В `Application.php` метод `getStatistics()` уже кэширует результат в памяти.

Для кэширования между запросами используйте Redis/Memcached (опционально):

```php
// Пример с Redis (требует установки расширения)
$redis = new Redis();
$redis->connect('127.0.0.1', 6379);

$stats = $redis->get('site_stats');
if (!$stats) {
    $stats = $app->getStatistics();
    $redis->setex('site_stats', 300, json_encode($stats)); // кэш на 5 минут
}
```

---

## Шаг 8: Создание новой страницы (пример)

Создайте файл `my_page.php`:

```php
<?php
/**
 * Моя новая страница
 */

// 1. Подключаем bootstrap
require_once __DIR__ . '/includes/bootstrap.php';

// 2. Проверяем авторизацию (если нужно)
if (!$auth->isLoggedIn()) {
    header("Location: login.php");
    exit;
}

// 3. Получаем данные
$user = $auth->getUser();
$config = $app->getConfig();

// 4. Бизнес-логика
$pageTitle = $lang->get('my_page_title');

// 5. Подключаем header
require_once __DIR__ . '/templates/header.php';
?>

<!-- HTML контент -->
<div class="container">
    <h1><?= htmlspecialchars($pageTitle) ?></h1>
    <p><?= htmlspecialchars($lang->get('welcome')) ?>, <strong><?= htmlspecialchars($user['username']) ?></strong>!</p>
</div>

<?php 
// 6. Подключаем footer
require_once __DIR__ . '/templates/footer.php'; 
?>
```

---

## Шаг 9: Добавление нового класса

1. **Создайте класс** в папке `includes/`:

```php
<?php
// includes/MyService.php
declare(strict_types=1);

class MyService
{
    private PDO $pdo;
    
    public function __construct(PDO $pdo)
    {
        $this->pdo = $pdo;
    }
    
    public function doSomething(): array
    {
        // Ваша логика
        return [];
    }
}
```

2. **Добавьте в autoloader** (`includes/autoloader.php`):

```php
$classMap = [
    // ... существующие классы ...
    'MyService' => __DIR__ . '/MyService.php',
];
```

3. **Используйте класс**:

```php
<?php
require_once __DIR__ . '/includes/bootstrap.php';

$myService = new MyService($pdo);
$result = $myService->doSomething();
```

---

## Чеклист миграции

- [ ] Резервная копия создана
- [ ] Bootstrap.php подключен во всех нужных файлах
- [ ] Удалено дублирование инициализации
- [ ] Проверены все основные страницы
- [ ] Проверена авторизация
- [ ] Проверена работа форума
- [ ] Проверена загрузка торрентов
- [ ] Логи PHP не содержат ошибок
- [ ] Производительность не упала (или улучшилась)
- [ ] Старый код закомментирован/удален

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи PHP
2. Включите `error_reporting(E_ALL)` временно
3. Проверьте, что все пути к файлам правильные
4. Убедитесь, что `ROOT_PATH` определен корректно
5. Проверьте права доступа к файлам (755 для папок, 644 для файлов)

---

## Откат изменений (если что-то пошло не так)

```bash
# 1. Восстановите резервные копии
git checkout .  # если используете git

# 2. Или вручную:
rm includes/bootstrap.php
rm includes/autoloader.php
rm includes/Application.php
rm includes/Config.php

# 3. Верните старые версии файлов из бэкапа
```

---

**Успешной миграции! 🚀**

